# kvfile

Key-Value File Reader/Writer

---

## Installation

Add to your `pawn.json`:

```json
{
    "dependencies": [
        "Southclaws/samp-kvfile"
    ]
}
```

Include in source:

```pawn
#include <kvfile>
```
